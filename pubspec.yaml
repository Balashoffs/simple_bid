name: simple_bid
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  calendar_date_picker2: ^1.1.8
  dropdown_button2: ^2.3.9
  flutter_bloc: ^9.0.0
  hydrated_bloc: ^10.0.0
  get_it: ^8.0.3
  collection: ^1.19.1
  intl: ^0.20.2
  dart_date: ^1.4.0
  bot_toast: ^4.1.3
  flutter_list_view: ^1.1.29
  pull_to_refresh: ^2.0.0
  flutter_svg: ^2.0.17
  sm_modules:
    git:
      url: https://github.com/Balashoffs/sm_modules
      ref: master

  # storages
  path_provider: ^2.1.5

  # generators annotation
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  shared_preferences: ^2.5.1
  dartz: ^0.10.1

  equatable: ^2.0.7
  uuid: ^4.5.1

  # network
  url_launcher: ^6.3.1
  file_picker: ^8.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^2.5.8
  intl_utils: ^2.8.9
  build_runner: ^2.4.14
  json_serializable: ^6.9.3

dependency_overrides:
  intl: ^0.19.0
  collection: ^1.19.0
  intl_utils: ^2.8.6
  file_picker: ^8.1.0
  analyzer: ^6.0.0
flutter:

  uses-material-design: true

